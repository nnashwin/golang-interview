package main

import (
	"fmt"
)

type LLNode struct {
	val  string
	Next *LLNode
}

func BuildLL(strs []string) (root LLNode) {

}

func IsInGraph(root LLNode, str string) (containsStr bool) {
	return
}
